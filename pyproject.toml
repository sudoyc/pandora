[project]
name = "workspace"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "beautifulsoup4>=4.14.3",
    "httpx>=0.28.1",
    "rich>=14.3.3",
    "textual>=8.2.1",
]

[project.scripts]
exhentai-dl = "cli:main"

[dependency-groups]
dev = [
    "pytest>=9.0.2",
]
