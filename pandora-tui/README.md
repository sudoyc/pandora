# pandora-tui Archive Note

`pandora-tui/` is archived and frozen.

The Rust TUI is kept in the repository as a historical implementation and REST/WebSocket consumer reference. It is no longer part of the active development path.

Do not add features, visual polish, or routine dependency updates here. Preserve API confidence through daemon/CLI contract tests under `tests/pandora_daemon/` instead.
